This folder contains HTML files which will be displayed in the browser. They are templated - we reference here variables and in their place we will insert values of those variables when displaying the HTML file in the browser.

Which .ejs file to use for which endpoint and what values to insert is defined in scripts in the "routes" folder.